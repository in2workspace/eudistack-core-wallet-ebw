name: license-gate

on:
  pull_request:
    branches:
      - main
      - 'release/v*'

permissions:
  contents: read

jobs:
  license-compliance:
    uses: ./.github/workflows/reusable-license-gate.yml
